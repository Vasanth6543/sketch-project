# Pencil Sketch Project 🖼✏️

This is a simple Python project that converts an image to a pencil sketch using OpenCV.

## 🔧 Requirements

- Python 3.x
- OpenCV library

Install OpenCV using:
```bash
pip install opencv-python
